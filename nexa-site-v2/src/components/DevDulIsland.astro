<a class="devdul-island" href="#top" aria-label="Return to NeXa home">
  <span class="devdul-text">DevDul</span>
</a>

<style>
  .devdul-island {
    position: fixed;
    top: var(--island-top);
    left: 50%;
    z-index: var(--z-island);
    display: inline-flex;
    min-height: 42px;
    align-items: center;
    padding: 8px 15px;
    border: 1px solid var(--color-line);
    border-radius: 999px;
    background: rgba(9, 13, 20, 0.72);
    box-shadow: var(--shadow-soft);
    color: var(--color-text);
    text-decoration: none;
    backdrop-filter: blur(18px);
    transform: translateX(-50%);
  }

  .devdul-text {
    font-size: 0.86rem;
    font-weight: 750;
  }

  body.content-active .devdul-island {
    transition:
      opacity 220ms ease,
      transform 220ms ease,
      filter 220ms ease;
  }

  body.content-active.flow-bar-docked .devdul-island {
    opacity: 0;
    pointer-events: none;
    filter: blur(4px);
    transform: translate(-42vw, -8px) scale(0.92);
  }
</style>
