# Hardware

This folder contains public-safe notes about the hardware side of NeXa RoVe.

The hardware documentation should stay general. It can explain what kinds of components are being explored, but it should not include exact private wiring, sensitive configuration, serial details, private calibration data, or anything needed to reproduce the private runtime.
